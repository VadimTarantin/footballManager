<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <context:annotation-config />

    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:application.properties</value>
            </list>
        </property>
    </bean>

    <bean id="wideTableTeamParser" class="com.football.manager.service.parser.impl.WideTableTeamParser">
        <property name="startTag" value="${tables.parser.start.tag}"/>
        <property name="endTag" value="${tables.parser.end.tag}"/>
        <property name="entitiesToCut" value="${tables.parser.entities.to.cut}"/>
    </bean>

    <bean id="formTableTeamParser" class="com.football.manager.service.parser.impl.FormTableTeamParser">
        <property name="startTag" value="${tables.parser.start.tag}"/>
        <property name="endTag" value="${tables.parser.end.tag}"/>
        <property name="entitiesToCut" value="${tables.parser.entities.to.cut}"/>
    </bean>

</beans>